FROM mcr.microsoft.com/playwright:v1.59.1-noble
WORKDIR /app
ENV NODE_ENV=production
ENV WORKER_BROWSER=state
ENV HEADLESS=true
COPY package*.json ./
RUN npm ci
COPY . .
CMD ["sh", "-lc", "npm run state:restore && npm run sync-worker"]
